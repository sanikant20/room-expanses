# Base URL for backend API requests. Defaults to "/api" (same-origin dev proxy).
# Set the full backend URL when the frontend is served from a different origin,
# e.g. VITE_BASE_URL=https://api.example.com/api
VITE_BASE_URL=
