# Template for Render (Static Site) — copy to .env.production and fill in.
# Frontend vars are baked into the JS bundle at build time — never put secrets here.
VITE_BASE_URL=https://<your-backend>.onrender.com/api
