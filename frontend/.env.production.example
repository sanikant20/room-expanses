# Template for production (single-origin deployment).
#
# The backend Web Service builds this frontend and serves it from its own
# origin, so axios MUST use the relative default "/api" — do NOT set
# VITE_BASE_URL to an absolute URL in production.
# Frontend vars are baked into the JS bundle at build time — never put secrets here.
# VITE_BASE_URL=
